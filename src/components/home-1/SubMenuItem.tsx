import FlexBox from "@component/FlexBox";
import styled from "styled-components";


export const SubmenuItem = styled(FlexBox) `
    &:hover {
        background: #eeeeee;
    }
    p {
        padding-left: 0.7rem;
    }
`